from django.apps import AppConfig


class AboutConfig(AppConfig):
    """
    Configuration for the 'about' app in Django project.
    Settings related to the 'about' app.
    """
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'about'
